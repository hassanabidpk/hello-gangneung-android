<resources>

    <style name="GoTheme" parent="AppTheme" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="FullscreenTheme" parent="GoTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/white</item>
    </style>

    <style name="GoTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <!--<item name="windowTranslucentStatus">true</item>-->
        <item name="windowNoTitle">true</item>

    </style>

    <style name="GoTheme.LandingPage.Title" parent="android:TextAppearance">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontPath">fonts/Roboto-Regular.ttf</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GoTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="GoTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GoTheme.Title" parent="android:TextAppearance">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:lines">2</item>
        <item name="android:maxLines">2</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="fontPath">fonts/Roboto-Regular.ttf</item>
    </style>

    <style name="GoTheme.Subtitle" parent="android:TextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/black_overlay</item>
        <item name="fontPath">fonts/Roboto-Regular.ttf</item>
        <item name="android:layout_marginStart">@dimen/text_margin_list_item</item>
        <item name="android:layout_marginLeft">@dimen/text_margin_list_item</item>
        <item name="android:layout_gravity">center_vertical</item>

    </style>

    <style name="GoTheme.Secondary.Title" parent="android:TextAppearance">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontPath">fonts/Roboto-Regular.ttf</item>
        <item name="android:layout_marginStart">@dimen/text_margin_list_item</item>
        <item name="android:layout_marginLeft">@dimen/text_margin_list_item</item>
        <item name="android:layout_gravity">center_vertical</item>

    </style>

</resources>
